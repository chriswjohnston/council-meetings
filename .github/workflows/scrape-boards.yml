name: Scrape Boards & Committees
on:
  schedule:
    - cron: '0 6 * * *'   # Daily at 6 AM UTC
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'scrape_boards.py'
      - 'build_index.py'

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4.2.2
        with:
          ref: main

      - uses: actions/setup-python@v5.4.0
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4

      - name: Run board scraper
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scrape_boards.py
        # writes docs/boards-data.json and triggers build_index.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/boards/ docs/boards-data.json docs/index.html
          git diff --staged --quiet && echo "No changes" || (
            git commit -m "Boards update: $(date +'%Y-%m-%d %H:%M')"
            git pull --rebase origin main
            git push origin main
          )
